<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#101322"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="16dp"
    android:paddingEnd="24dp"
    android:paddingBottom="12dp"
    tools:context=".fragments.internalFragments.settingFragments.CancionesOcultasFragment">

    <TextView
        android:id="@+id/tvCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="14dp"
        android:letterSpacing="0.08"
        android:text="Canciones ocultas [0]"
        android:textAllCaps="true"
        android:textColor="#9AA3B2"
        android:textSize="11sp" />

    <LinearLayout
        android:id="@+id/layoutControlesOcultas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="18dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:background="@drawable/bg_busqueda_input">

            <ImageView
                android:id="@+id/ivIconoBuscarOcultas"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center_vertical|start"
                android:layout_marginStart="16dp"
                android:contentDescription="Buscar"
                android:src="@drawable/ic_search"
                android:tint="#8E9193" />

            <EditText
                android:id="@+id/etBuscadorOcultas"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginStart="48dp"
                android:layout_marginEnd="12dp"
                android:background="@android:color/transparent"
                android:hint="Buscar canción oculta..."
                android:imeOptions="actionSearch"
                android:inputType="text"
                android:singleLine="true"
                android:textColor="#E3E0F2"
                android:textColorHint="#8E9193"
                android:textSize="16sp" />

        </FrameLayout>

        <ImageButton
            android:id="@+id/btnRecargarOcultas"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_busqueda_boton"
            android:contentDescription="Limpiar búsqueda"
            android:padding="16dp"
            android:src="@drawable/ic_refresh"
            android:tint="#E3E0F2" />

    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="4dp"
            android:paddingBottom="24dp"
            tools:listitem="@layout/viewholder_cancion" />

        <TextView
            android:id="@+id/tvMensajeOcultas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:padding="24dp"
            android:text="No hay canciones ocultas"
            android:textColor="#E3E0F2"
            android:textSize="17sp"
            android:textStyle="bold"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>